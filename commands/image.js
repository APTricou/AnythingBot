const images = {}

// msg in format '+img name url' to add a pic to database
// msg in format '+img name' to recall img
// msg in format '+img random' to recall random img

module.exports = (args, msg) => {
  function add(args, msg) {}
}
